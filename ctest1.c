
void drawPoint(SDL_Renderer *renderer, double zoom_factor, double x, double y) {
  int translated_x = (x + (1000/2) * zoom_factor) / zoom_factor;
  int translated_y = (y + (1000/2) * zoom_factor) / zoom_factor;
  SDL_RenderDrawPoint(renderer, translated_x, translated_y);
}
void drawPoint(SDL_Renderer *renderer, double zoom_factor, double x, double y) {
  int translated_x = (x + (1000/2) * zoom_factor) / zoom_factor;
  int translated_y = (y + (1000/2) * zoom_factor) / zoom_factor;
  SDL_RenderDrawPoint(renderer, translated_x, translated_y);
}
void drawPoint(SDL_Renderer *renderer, double zoom_factor, double x, double y) {
  int translated_x = (x + (1000/2) * zoom_factor) / zoom_factor;
  int translated_y = (y + (1000/2) * zoom_factor) / zoom_factor;
  SDL_RenderDrawPoint(renderer, translated_x, translated_y);
}
void drawPoint(SDL_Renderer *renderer, double zoom_factor, double x, double y) {
  int translated_x = (x + (1000/2) * zoom_factor) / zoom_factor;
  int translated_y = (y + (1000/2) * zoom_factor) / zoom_factor;
  SDL_RenderDrawPoint(renderer, translated_x, translated_y);
}

typedef uint_fast32_t index;
typedef uint_fast32_t index;
typedef uint_fast32_t index;
typedef uint_fast32_t index;
